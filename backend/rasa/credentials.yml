# REST channel (pour le chatbot web)
rest:

# Socket.IO channel (pour le widget web)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Configuration WhatsApp via Twilio (optionnel)
# twilio:
#   account_sid: "YOUR_ACCOUNT_SID"
#   auth_token: "YOUR_AUTH_TOKEN"
#   twilio_number: "+1234567890"

# Configuration Facebook Messenger (optionnel)
# facebook:
#   verify: "YOUR_VERIFY_TOKEN"
#   secret: "YOUR_APP_SECRET"
#   page-access-token: "YOUR_PAGE_ACCESS_TOKEN"

# Configuration Telegram (optionnel)
# telegram:
#   access_token: "YOUR_BOT_TOKEN"
#   verify: "YOUR_VERIFY_TOKEN"
#   webhook_url: "https://your-domain.com/webhooks/telegram/webhook"
